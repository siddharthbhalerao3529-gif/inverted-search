# inverted-search
This project implements an inverted index in C, which allows fast lookup of words across multiple text files. An inverted index maps each word to the files it appears in, along with the count of occurrences per file.
